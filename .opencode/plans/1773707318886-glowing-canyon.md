# Plan

Goal: finish merchant-room/shop flow so the agent can leave shop inventory, return to merchant_room, then use normal proceed to continue the run.

## Recommended approach

1. Update merchant state modeling in `Bridge/StateExporter.cs`
   - set merchant inventory input readiness from `enter_shop_available || leave_available || proceed_available`
   - keep `scene` split as `shop` for inventory and `merchant_room` for room shell

2. Update merchant context in `Bridge/BridgeIntrospection.cs`
   - export `leave_available` when `NMerchantInventory.IsOpen`
   - export `proceed_available` only when inventory is closed and merchant-room proceed button is visible
   - keep shop items empty when not inside inventory

3. Add explicit leave action in `Bridge/BridgeIntrospection.cs` and `Bridge/CommandProcessor.cs`
   - implement `TryLeaveMerchant()` via `NMerchantInventory.Close()` or back-button equivalent if needed
   - add `leave_shop` command path
   - leave `proceed` for merchant-room continue only

4. Verify merchant-room proceed still works in `Bridge/BridgeIntrospection.cs`
   - after `leave_shop`, confirm `TryProceed(runState)` uses merchant-room proceed button path
   - if direct button release is flaky, fall back to the room method that the button triggers

5. Keep current shop export stable
   - preserve gold-aware `affordable` / `purchasable` flags for cards, relics, potions, and removal
   - do not change purchase flow unless needed by merchant leave/proceed testing

## Critical files

- `Bridge/BridgeIntrospection.cs`
- `Bridge/StateExporter.cs`
- `Bridge/CommandProcessor.cs`
- `Bridge/ExportHooks.cs`

## Verification

1. Rebuild/deploy with `./build-and-deploy.cmd` from PowerShell
2. Enter merchant room and confirm `state.json` shows:
   - `scene: "merchant_room"`
   - `merchant.enter_shop_available: true`
   - `merchant.proceed_available: true`
3. Open shop and confirm `state.json` shows:
   - `scene: "shop"`
   - `merchant.leave_available: true`
   - `merchant.proceed_available: false`
4. Issue `leave_shop` and confirm state returns to `merchant_room`
5. Issue `proceed` and confirm state advances off merchant room to the next map/room state
6. Re-check `godot.log` for merchant/shop transition errors

## Unresolved questions

- none
